<%= form_for(monitored_url) do |f| %>
  <% if monitored_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(monitored_url.errors.count, "error") %> prohibited this monitored_url from being saved:</h2>

      <ul>
      <% monitored_url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="field">
    <%= f.label :active %>
    <%= f.check_box :active %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
